# Server port
PORT=8080

# Frontend URL for CORS (set to your deployed frontend URL in production)
ALLOWED_ORIGIN=http://localhost:5173

# Path to built React static files (leave empty in dev, set in production)
# STATIC_DIR=/public
